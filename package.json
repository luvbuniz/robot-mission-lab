{
  "name": "robot-mission-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive middle-school robotics course by Amy Sullivan",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=24"
  }
}
